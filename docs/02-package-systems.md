# 02 — Package Systems

Ubuntu uses multiple package systems.

Primary:
- APT (traditional Debian packages)
- Snap (containerized applications)

Verify both are available before installing software.

## Check APT

apt --version

## Check Snap

snap version

# If snap is missing (rare on Ubuntu Desktop):
# sudo apt install -y snapd

